# Exercise App 2 (React Native)

This project is now a React Native app using Expo and React Native Elements.

## Available scripts

- `npm start` - start Expo dev server
- `npm run android` - run on Android
- `npm run ios` - run on iOS
- `npm run web` - run on web via Expo
- `npm run build` - export web build
